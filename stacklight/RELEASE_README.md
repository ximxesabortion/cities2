# Stacklight 0.2.5 release package

The `Mod/Stacklight` folder contains the runtime files. `StoreListing` contains
the publishing art and metadata. `Source` contains the corresponding source
release.

The included install archive is provided for backup and review. The supported
distribution channel is the subscribed live Paradox Mods package.
